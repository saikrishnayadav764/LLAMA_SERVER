{
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/transcribe", "dest": "/server.js" },
    { "src": "/transcriptions", "dest": "/server.js" }
  ]
}
